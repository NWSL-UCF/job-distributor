{
  "_comment": "config.json is no longer read by start.py. Pass all settings as CLI arguments.",
  "_example": "python start.py --expId mnist_tune --server_port 5000 --dashboard_port 5050 --workspace_path /data/experiments",
  "_notes": {
    "jobDB": "Always jobs.db (fixed)",
    "host": "Always localhost (fixed)",
    "parameters": "Configured from the dashboard UI after startup",
    "idleTimeout": "Configured from the dashboard UI after startup",
    "abortedJobResetTimeout": "Configured from the dashboard UI after startup"
  }
}
